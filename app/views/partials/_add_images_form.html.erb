<%= form_with(model: admin_image, local: true) do |form| %>
  <div class="field">
    <%= form.label 'Dodaj zdjęcia' %>
    <%= form.file_field :images, multiple: true, :required => true %>
  </div>
  <div class="actions">
    <%= form.submit 'Dodaj zdjęcia' %>
  </div>
<% end %>
